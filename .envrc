# Cached dev shell, loaded on cd. Per-command `nix develop -c` re-copies the
# working tree into the store on every edit; direnv re-evaluates only when
# flake.nix or flake.lock change. See build/README.md.
use flake
